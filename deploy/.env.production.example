# MedHarness v0.5.0-edge production environment template
# Copy to .env.production and adjust before running docker compose.
# .env.production must stay gitignored and out of the Docker build context.

VERSION=0.5.0-edge
