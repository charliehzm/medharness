# MedHarness Console · 产品详细设计文档

> **状态**：v0.6 产品详细设计（draft · 已按"合规 + 安全网关"定位对齐）
> **定位锚点**：[unified-gateway.md](../architecture/unified-gateway.md) 架构 · 本文聚焦"产品形态 + 控制台 UX"
> **配套原型**：[prototype/console-demo.html](../../prototype/console-demo.html)（单文件可点击 · 浏览器直开）
> **维护**：技术委员会 + 合规委员会

---

## 0. 产品定位（锁定）

> **MedHarness = 医疗 LLM 流量的「合规 + 安全」网关。**
> 不管是开发者用 Claude Code / Codex 写代码，还是业务系统用 Dify / ComfyUI 跑生产工作流——只要它调 LLM，流量就必经同一个网关。**MedHarness Console 是这个网关的控制台**：让 CTO 一眼看懂今天合规且安全、让合规官 4 小时交监管包、让工程师不碰 YAML 改策略。

### 0.1 三条定位边界（已拍板，作为设计约束）

| 维度 | 取舍 | 含义 |
|---|---|---|
| **行业** | 医疗垂直，**不去医疗化** | HIPAA + PIPL + 健康医疗数据安全指南是护城河 · PHI 是一等公民，不是"敏感数据的一类" |
| **场景** | "通用" = **跨场景**（开发期 + 生产期），**非跨行业** | 一个 IDE/平台无关的网络网关，同时罩住 dev（Claude Code/Codex）与 prod（Dify/ComfyUI/自研） |
| **范畴** | **合规 + 安全**，不止合规 | 合规（满足监管）+ 安全（防攻击/防滥用/防输出事故）双轮 |

### 0.2 北极星 vs 现状（诚实护栏）

产品对外讲愿景，但界面与文档**绝不把未建能力标成已完成**：

```
北极星：医疗 LLM 流量的合规 + 安全网关 · 开发期 ↔ 生产期
────────────────────────────────────────────────────────
✅ 今天 (v0.5.0-edge)：开发期 AI Coding 合规内核
   —— 4 闸门 + WORM 审计 + 容器化（IDE Hook 强制）
🚧 演进中 (v0.6)：IDE 无关的网络网关 + 生产期安全
   —— 协议归一化 / 流式 SSE / 输出安全扫描 / 多模态 PHI / 滥用防护
```

**Console 设计原则**：可以**先于后端**把目标形态画全（原型/UX 验证），但凡渲染"演进中"能力的视图，必须打 `🚧 v0.6` 标，并在数据源接通前以合成 mock 标注——绝不让 demo 变成过度承诺。

---

## 1. 它解决什么 · 威胁模型（合规 + 安全 × 开发期 + 生产期）

产品价值 = 在 LLM 流量的咽喉点，挡住两类问题：**合规问题**（监管罚你）与**安全问题**（攻击者/事故坑你）。生产期把合规问题**放大**，并**新增**安全问题。

### 1.1 威胁矩阵（这就是 Console 要可视化的东西）

| 威胁 | 类别 | 方向 | 开发期 | 生产期 | 现有能力 | 缺口（商业化新建） |
|---|---|---|---|---|---|---|
| PHI 入站泄漏 | 合规 | 入站 | 中 | 极高 | ✅ 闸门一 phi-detector | 流式 SSE 边扫边转 |
| PHI 主动带入 | 合规 | 入站 | 中 | 高 | ✅ 闸门二 desensitize | RAG 入库时脱敏 |
| 模型越权 / 出境 | 合规 | 入站 | 中 | 高 | ✅ 闸门三 model-router | （直接复用） |
| 审计可重放 | 合规 | 全程 | 高 | 极高 | ✅ audit-log WORM | （直接复用） |
| **多模态 PHI** | 合规 | 入站 | 低 | **高** | ❌ 无 | DICOM/影像/OCR 脱敏（Phase C·最难） |
| **RAG prompt 注入** | 安全 | 入站 | 低 | **极高** | 🟡 第四层（dev 向·25 case） | 生产注入 corpus + 来源分级隔离 |
| **LLM 输出安全** | 安全 | **出站** | 低 | **高** | ❌ 无 | 出站扫描：PHI 回流 / 有害内容 / 幻觉医嘱 |
| **调用滥用 / 成本** | 安全 | 入站 | 低 | 中 | 🟡 rate limiter（per role） | 生产配额 + 异常调用检测 |

加粗行 = 生产期才真正凸显、且基本是新建——这就是商业版相对开源版的**增量清单**。

### 1.2 一个关键产品洞察：流量是**双向**的

开源版只管"入站"（开发者 prompt 出站到 LLM 前扫 PHI）。生产期必须管**出站**——**LLM 的回答**同样危险：

- 模型把 RAG 上下文里的 PHI **原样回吐**给患者端
- 模型**幻觉**出一条不存在的医嘱/诊断，直接进了业务系统
- 模型被注入后**输出有害内容**

所以生产网关是**双向闸门**：请求方向扫 PHI + 路由，响应方向扫输出安全。Console 的流量监控必须画出这条回路——这是与开源版最直观的差异点。

---

## 2. 目标用户与核心诉求（Job-to-be-Done）

> 10-30 人医疗 SaaS 通常没有独立 SecOps——安全维度由 CTO + 工程师 + 合规官分担，**不另设第 4 个主角色**（避免设计出客户没有的岗位）。

| 角色 | 一句话画像 | 最怕什么 | 来 Console 要什么 | 默认首屏 |
|---|---|---|---|---|
| **CTO / 技术负责人** | 为合规与安全结果背锅的人 | 监管罚款 4% · 生产出 LLM 事故上新闻 | **一个能信的数字**：今天合规且安全吗？ | 🏠 态势 |
| **合规官 / Compliance Officer** | 监管来了第一个被问的人 | 4 小时交不出审计包 | 任意一条流量的**完整血缘 + 哈希链 + 安全事件** | 🔍 审计追溯 |
| **工程师 / Harness Eng** | 接上游、配阈值、修红灯的人 | 改错配置开后门 · 注入/滥用没拦住 | **不碰源码**改合规与安全策略 + 看双向流量 | ⚙️ 策略配置 |

每个角色都横跨 **dev + prod 两个场景**：CTO 要在一块屏上看到开发期与生产期分层的健康；工程师既配 dev 上游也配 prod 上游。

---

## 3. 设计铁律（6 条）

| # | 铁律 | 为什么 | 落地 |
|---|---|---|---|
| 1 | **一个数字定生死** | CTO 没空看 20 个指标 | 首屏最大元素 = **综合健康分 0-100**（下钻拆成合规分 + 安全分） |
| 2 | **角色化默认视图** | 不同人来要的不同 | 顶部角色切换 → 默认页随之变 |
| 3 | **渐进披露三层** | 一屏塞满 = 没人看 | 概览卡片 → 钻取 → 详情抽屉 |
| 4 | **零术语门槛** | "PHI recall 1.0" 业务听不懂 | "患者信息泄漏拦截率 100%"；"prompt injection"→"恶意指令注入" |
| 5 | **每个红灯给出路** | 报警不给方案 = 焦虑制造机 | 每个红灯卡带 `[点这里看怎么修]` → runbook |
| 6 | **🛡️ 永不显示原始 PHI** | Console 自己不能成泄漏点 | 顶栏常驻"本页 0 PHI"绿标 · 全程只显示占位符 + 哈希 + 聚合数 |
| 7 | **🟪 合规与安全分色不混淆** | 两类问题处置路径不同 | 合规=青绿、安全=紫罗兰、状态=红黄绿 · 视觉上一眼区分"是漏 PHI 还是被攻击" |

> 铁律 6 是红线，铁律 7 是因加了"安全"维度而新增的：合规问题（漏 PHI）走脱敏/阻断，安全问题（被注入/被滥用）走隔离/熔断——处置不同，界面就要分色，免得 CTO 把两类问题混为一谈。

---

## 4. 信息架构

```
MedHarness Console
├── 🏠 态势            ← CTO 落点 · 综合健康分(合规分+安全分) + 7 日趋势 + 红灯清单
├── 📊 流量监控        ← 杀手锏 · 双向桑基图(入站 PHI + 出站输出安全) + 双色事件流
├── 🔍 审计追溯        ← 合规官落点 · 血缘图(含出站段) + 哈希链 + 安全事件 + 监管包
├── 🚦 研发流水线       ← 工程师落点 · change 看板(仅开发期)
├── ⚙️  策略配置        ← 配置中心 · 合规策略 + 安全策略 + diff 预览 + 审批流
├── 🔌 上游接入        ← 接入向导 · 3 步生成 base_url + key(dev/prod)
└── 📋 报表中心        ← 季度合规+安全报表 + 监管应对包导出
```

**导航即权限**：工程师能改阈值但不能会签；合规官只读流量不能接上游；高危项灰显而非隐藏（让用户知道"有这能力，只是我没权限"）。

---

## 5. 核心页面详细设计

### 5.1 🏠 态势（CTO 落点）

**唯一目的**：3 秒回答"我今天合规**且安全**吗"。一个综合分，下钻见合规/安全两个子分。

```
┌──────────────────────────────────────────────────────────────┐
│  综合健康分                                                     │
│        ╭─────────╮       合规分 96 🟢   安全分 91 🟡            │
│        │   94    │  🟢   ├─ PHI 拦截 100%   ├─ 注入阻断 100%    │
│        │ / 100   │ 健康  ├─ 脱敏 正常       ├─ 输出扫描 🚧v0.6  │
│        ╰─────────╯       ├─ 路由 11/11      ├─ 多模态 🚧v0.6   │
│   较昨日 +2 · 本周 0 漏出 └─ 审计链 完整     └─ 滥用 1 次熔断    │
│                                                                │
│  ── 合规闸门 ──────────────  ── 安全防线 ──────────────────    │
│  🟢 闸门一 PHI 扫描 100%     🟢 注入防御 100%                   │
│  🟢 闸门二 脱敏 0.02ms       🚧 输出安全扫描（v0.6 演进中）      │
│  🟢 闸门三 路由 11/11        🚧 多模态 PHI（v0.6 · DICOM/影像）  │
│  🟢 审计 WORM 链完整         🟡 滥用防护 今日熔断 1 次           │
│                                                                │
│  ⚠️ 需要注意（1）                                              │
│  🟡 prod-comfyui 今日 1 次调用滥用熔断 → [点这里看怎么修]       │
└──────────────────────────────────────────────────────────────┘
```

**设计要点**：
- 综合分是绝对最大元素（铁律 1）；右侧把它拆成**合规分 + 安全分**两条，各自下挂明细
- 闸门分两组：合规（青绿）/ 安全（紫罗兰），呼应铁律 7
- `🚧 v0.6` 明确标出未建能力（铁律 0.2 诚实护栏），不冒充已完成
- 健康分算法可解释：`100 − PHI漏出×50 − 安全事件未处置×20 − 高危配置未审批×10 − 闸门/防线降级×5`

### 5.2 📊 流量监控（产品杀手锏 · 双向桑基）

**这是销售/融资 demo 核心**——让客户**亲眼看到** PHI 被一层层拦下、**且** LLM 的回答也被回扫。

**入站桑基（请求方向）**：上游 → PHI 扫描 → 脱敏 → 路由 → 下游
**出站桑基（响应方向 · 生产网关特有）**：下游 → 输出扫描（PHI 回流 / 有害 / 幻觉）→ 上游

```
入站 ▶                                            ◀ 出站 (🚧 v0.6)
上游          闸门(合规)                下游          输出扫描(安全)
┌────────┐                                          ┌──────────────┐
│Dify RAG│━━┓  ┌──────────────┐   ┏━→ 私有 Qwen ──→│ PHI 回流 ✓ 0  │
│1.2k/h  │  ┣━→│ PHI 扫描+脱敏 │━━━┫               │ 有害内容 ✓ 0  │
└────────┘  ┃  │ 命中47→脱敏   │   ┗━→ 境外(脱敏12) │ 幻觉医嘱 🟡 2 │
┌────────┐  ┃  │ 拦截3→阻断🔴  │                    └──────────────┘
│ComfyUI │━━┫  └──────────────┘    🟪 注入拦截 8（RAG 来源）
│380/h   │  ┃                       🟪 滥用熔断 1（comfyui 突发）
└────────┘  ┛   ✗ 阻断3(未脱敏L4影像 · 🚧多模态v0.6)

双色实时事件流（合规=青绿 / 安全=紫）
🟢 14:23:01 dify-rag    [合规] L2 脱敏后路由 qwen-max      routing#a1b2
🟪 14:23:00 dify-rag    [安全] RAG 来源含恶意指令 → 隔离   inject#c3d4
🔴 14:22:59 comfyui     [合规] L4 未脱敏 → 阻断            阻断#e5f6
🟡 14:22:58 prod-bot    [安全] 输出疑似幻觉医嘱 → 标记复核  output#g7h8
```

**设计要点**：
- 顶部 `[全部] 开发期 生产期` 三态过滤——展示"一个网关通吃两个场景"
- 入站/出站**两条桑基**，出站段打 `🚧 v0.6` 标
- 桑基"流"用 CSS 动画粒子；阻断粒子撞到红闸门停下
- 事件流**双色**：合规事件青绿、安全事件紫——一眼区分"漏 PHI"还是"被攻击"（铁律 7）
- 全程仅占位符——demo 时客户问"那张三呢"，答："你在我界面上永远看不到张三，闸门二第 ④ 步就加密成 `__NAME_a1__` 了"（铁律 6）

### 5.3 🔍 审计追溯（合规官落点）

**唯一目的**：监管说"回放某条调用"，4 小时交包。血缘图**含出站段 + 安全事件**。

```
┌──────────────────────────────────────────────────────────────┐
│ 审计追溯       搜索[routing#a1b2___]🔍   [导出监管包]          │
├──────────────────────────────────────────────────────────────┤
│ 血缘图（一条流量走完全程 · 双向）                               │
│ 入站: 上游请求→闸门一命中→闸门二加密→闸门三路由→下游            │
│ 出站: 下游响应→输出扫描(PHI回流✓/幻觉🟡)→回上游  🚧v0.6        │
│ 安全: 🟪 该请求 RAG 来源经注入扫描 ✓ 无恶意指令                 │
│        哈希链 ✓ 完整（block #18,432）                          │
│                                                                │
│ 调用详情                                                       │
│ • prompt(脱敏): "帮我看下 __NAME_a1__ __ID_b2__ 的检验异常项"   │
│ • response(脱敏+输出扫描): "...建议复查，未检出 PHI 回流"        │
│ • 模型: qwen-max-2026-01(私有) · L3 · allowlist✓ · 异构✓        │
│ • 当前哈希: 9f3a...c4d2  ← 上一条: 7b1e...a8f0                  │
└──────────────────────────────────────────────────────────────┘
```

**设计要点**：血缘图横向分段对应双向闸门；`[导出监管包]` 一键生成 AUDIT_BUNDLE（prompt/response history 脱敏版 + model versions + routing/安全决策 + 哈希链）；仍然 0 PHI（prompt/response 均占位符版，原始反查走受控环境界外授权）。

### 5.4 ⚙️ 策略配置（配置中心 · 合规策略 + 安全策略）

**唯一目的**：不碰 YAML 改策略，且改不出后门。配置分**合规**与**安全**两组。

```
┌──────────────────────────────────────────────────────────────┐
│ 策略配置                              [YAML 视图] [保存草稿]    │
├──────────────────────────────────────────────────────────────┤
│ ─ 合规策略 ─        │  生产环境策略 (context=prod)             │
│ ▸ 上游接入          │  ┌────────────────────────────────────┐ │
│ ▾ 场景策略          │  │ PHI 命中处理                        │ │
│  ▶ prod ←           │  │  ○ 仅警告  ● 硬阻断  ○ 警告+脱敏    │ │
│ ▸ 模型白名单        │  │ 数据分级强制  ☑L3 ☑L4 ☐L2          │ │
│ ▸ PHI 字段          │  └────────────────────────────────────┘ │
│ ─ 安全策略 ─        │  ┌────────────────────────────────────┐ │
│ ▸ 注入防御          │  │ 注入防御     ● 阻断  ○ 警告          │ │
│ ▸ 输出安全🚧        │  │ 输出扫描🚧   ● 扫PHI回流 ☑幻觉标记  │ │
│ ▸ 调用配额          │  │ 调用配额     prod 200 QPS / 异常熔断 │ │
│ ─ 治理 ─            │  └────────────────────────────────────┘ │
│ ▸ 保留策略          │                                          │
│                     │  ⚠️ 影响 2 个生产上游 · 高危→合规会签     │
│                     │  变更预览(diff):                         │
│                     │   - block_on_injection: warn             │
│                     │   + block_on_injection: block            │
└──────────────────────────────────────────────────────────────┘
```

**配置 UX 三原则**：① 改前看影响（"影响 2 个生产上游"）② diff 预览（像 code review）③ 嵌入审批流（高危改完不直接生效 → 会签 → 落审计 → 可回滚）。

**配置治理矩阵**（界面据此决定按钮文案）：

| 配置类 | 改动权限 | 审批 | 按钮 |
|---|---|---|---|
| upstreams / rate_limit | 技术委员会 | 单签 | `[保存]` |
| policies / gates 阈值（含**注入/输出/配额**） | 技术委员会 | + 合规会签 | `[提交审批]` |
| allowlist 加模型 | 技术委员会 | + 合规会签 | `[提交审批]` |
| fields PHI 字段 | 合规委员会 | 单签 | `[保存]` |
| retention 缩短保留期 | 双委员会 + 法务 | 三签 | `[提交三签审批]` |

### 5.5 🔌 上游接入

3 步接入、零代码改造：选协议（openai/anthropic）→ 选场景（dev/prod）→ 生成 `base_url + key` → 复制给 Dify/ComfyUI/IDE。表格列：名称 / 场景 / 协议 / 状态 / 今日流量 / **PHI 拦截 + 安全事件**。

### 5.6 🚦 研发流水线（工程师落点 · 仅开发期）

复用 v0.5.0-edge change 看板（12 步 SOP + Step 0/10/12 合规闸门 + 红队 drill）。本文不展开，导航保留入口。

---

## 6. 视觉语言

| 元素 | 规范 |
|---|---|
| **主色** | 深蓝 `#0B1F3A`（信任/医疗）|
| **类别色** | 🟢 合规 `#0FB5A6`（青绿）· 🟪 安全 `#7C3AED`（紫罗兰）—— 铁律 7 |
| **状态色** | 健康 `#22C55E` / 注意 `#F59E0B` / 危险·阻断 `#EF4444` |
| **演进标** | `🚧 v0.6` 灰底标签，贴在未建能力上（诚实护栏） |
| **健康分** | 唯一允许的超大字号（96px+）· 圆环进度 |
| **数据展示** | 占位符 `__NAME_a1__`（等宽+浅青底）· 哈希 `routing#xxxx`（灰） |
| **动效** | 双向桑基粒子流 · 新事件淡入 · 阻断/熔断抖动 · 克制不炫技 |

---

## 7. 关键交互流程

### 7.1 demo 故事线（销售/融资 90 秒脚本 · 已含安全维度）
1. **态势** → "94 分，合规 96 安全 91，绿的" （铁律 1）
2. **流量监控** → 入站桑基粒子流动 → "这是你公司此刻真实的 LLM 流量" （杀手锏）
3. 指红色阻断 → "ComfyUI 想发一张没脱敏的 L4 影像，被合规闸门挡了"
4. 指紫色事件 → "这条是 RAG 知识库里被人塞了恶意指令，被安全防线隔离了" （安全维度，开源版没有）
5. 指出站段 → "连模型的**回答**我们都回扫——防它把患者信息原样吐回去、或幻觉出假医嘱" （生产特有）
6. 客户问"患者姓名呢" → 指占位符 → "你在我界面永远看不到张三，这是设计" （铁律 6 = 信任）
7. **审计追溯** → `[导出监管包]` → "监管来了，4 小时这包交出去"
8. **策略配置** → 改个开关 → diff + "需会签" → "改配置像 code review，改不出后门"

### 7.2 红灯 → runbook · 7.3 配置审批闭环
任意红/黄/紫卡片 → `[点这里看怎么修]` → 跳 runbook；高危配置改完 → diff → 提交 → 会签 → 生效+落审计 → 可回滚。

---

## 8. 数据边界与安全约束（红线）

| 约束 | 落地 |
|---|---|
| **永不显示原始 PHI** | 全界面只显示占位符 + 哈希 + 聚合数（含**出站 response** 也是脱敏版） |
| **本页 0 PHI 绿标** | 顶栏常驻 · 对客户/审计可见的承诺 |
| **原始反查需授权** | 走受控环境单独授权流，**不在 Console 界面内** |
| **Console 操作落审计** | 看了哪条审计、改了什么配置、导出哪个包 → 全落 audit-log |
| **角色最小权限** | 工程师不能会签、合规官不能接上游 · 不可用项灰显 |
| **安全事件不外泄细节** | 注入样本、攻击 payload 仅显示分类与处置，不在界面回显原文（防二次传播） |

---

## 9. 原型说明与待办

**当前原型** [prototype/console-demo.html](../../prototype/console-demo.html) 已实现：态势（健康分+闸门卡）/ 流量监控（入站桑基+事件流）/ 审计 / 配置 / 上游 / 角色切换 / 0 PHI 绿标。

**本次定位升级后，原型需同步（下一步）**：
- [ ] 态势：综合分下钻拆**合规分 + 安全分**；闸门卡分两组（合规青绿 / 安全紫）+ `🚧 v0.6` 标
- [ ] 流量监控：加**出站桑基段** + 事件流**双色**（合规青绿 / 安全紫）+ 注入/滥用/输出事件类型
- [ ] 配置：左侧导航分**合规策略 / 安全策略 / 治理**三组；新增注入/输出/配额开关
- [ ] 审计：血缘图加**出站段** + 安全事件行
- [ ] 全局：未建能力统一 `🚧 v0.6` 标，保持诚实护栏

> 原型同步是独立一步（UX 验证），与本设计文档解耦——文档先把目标形态定全，原型按此追平。

---

## 10. 与现有资产的关系 + 路线图（诚实）

| 现有（v0.5.0-edge 已建） | Console 角色 | 类别 |
|---|---|---|
| 闸门一/二/三 MCP 内核 | 态势"合规闸门"组 + 入站桑基 | 合规 |
| audit-log WORM + 哈希链 | 审计追溯页血缘 + 哈希链 | 合规 |
| prompt-injection-scan（dev 向） | 态势"安全防线·注入"卡 + 紫色事件 | 安全 |
| model-router rate limiter | 态势"安全防线·滥用"卡 | 安全 |
| MODEL_ALLOWLIST / 31 fields | 配置中心"模型白名单"/"PHI 字段" | 合规 |

| 新建（v0.6 · 商业化才需要） | 阶段 | 类别 |
|---|---|---|
| IDE 无关 egress gateway（协议归一化） | Phase A | 基础 |
| 流式 SSE PHI 扫描 + Console 流量数据管道 | Phase B | 合规+UX |
| **输出安全扫描**（出站：PHI 回流/有害/幻觉） | Phase B/C | **安全** |
| **多模态 PHI**（DICOM/影像/OCR） | Phase C（最难） | **合规** |
| RAG 注入生产 corpus + 来源分级隔离 | Phase C | **安全** |
| 生产配额 + 异常调用检测 | Phase B | **安全** |

**一句话**：开源版埋好了"内核可复用"的伏笔（L5 横切设计）；商业版长出"生产期不可绕的强制点"+"双向闸门"+"安全防线"——这是护城河，也是本设计文档围绕的增量。
